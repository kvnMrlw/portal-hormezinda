# URL da API do backend. Em desenvolvimento local pode deixar em branco
# (o codigo usa http://localhost:5000/api como padrao).
# Em producao, aponte para a URL do backend hospedado (ex.: Render):
# VITE_API_URL=https://seu-backend.onrender.com/api
VITE_API_URL=
